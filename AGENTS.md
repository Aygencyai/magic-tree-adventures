# magic-tree-adventures — Codex Instructions

This project already has Claude-specific instructions in `CLAUDE.md`. For Codex work:

1. Read `/Users/lraygency/aygency/AGENTS.md`.
2. Read this project's `CLAUDE.md` as the project-specific source for stack, commands, architecture, protected files, and gotchas.
3. Follow the nearest instruction if root and project guidance conflict.
4. Do not edit Claude-specific files unless the user explicitly asks.
5. For non-trivial work, create or update the project's phased Markdown plan before implementation.
6. Verify with the commands specified in `CLAUDE.md` after significant changes.

Use Aygency defaults from the root file: `pnpm`, TypeScript strict mode where applicable, deliberate UI systems, Brain-first reusable knowledge, and no secret values in output.

