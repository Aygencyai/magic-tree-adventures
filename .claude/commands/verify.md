Run the full verification suite for The Magic Tree Adventures:

1. Run `pnpm build` and report any errors
2. Search for off-palette colours (any hex codes not in the design system)
3. Search for wrong fonts (Inter, Roboto, Arial, system-ui references)
4. Check that all pages render without errors
5. Report results
