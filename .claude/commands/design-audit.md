Audit the specified component or page against The Magic Tree Adventures design system:

1. Check colours match the palette (parchment, bark, gold, forest, crystal, chakra accents)
2. Check typography uses Fraunces (headings), Nunito (body), Caveat (accents) only
3. Check spacing follows Tailwind scale consistently
4. Check animations use the softer easing [0.25, 0.8, 0.25, 1]
5. Check buttons use rounded-full with gold styling
6. Check sections use SectionContainer and SectionHeading components
7. Verify no pure white (#FFFFFF) or pure black (#000000) is used
8. Report any deviations from the design system
